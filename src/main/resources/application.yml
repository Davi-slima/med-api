spring:
  datasource:
    url: jdbc:postgresql://localhost/codechella_db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    format-sql: true

hibernate:
  dialect: org.hibernate.dialect.HSQLDialect
